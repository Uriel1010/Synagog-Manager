/* app/static/js/scripts.js */

// You can add custom JavaScript functionality here that applies globally or to specific pages.

// Example: Initialize Bootstrap tooltips if you decide to use them
/*
document.addEventListener('DOMContentLoaded', function () {
  var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
  var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
    return new bootstrap.Tooltip(tooltipTriggerEl)
  })
});
*/

// Add any other global JavaScript interactions here.
// Page-specific JavaScript is often better placed in its own file or within <script> tags
// in the specific template block (like the scanner.js example).

console.log("Global scripts.js loaded.");